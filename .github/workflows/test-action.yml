name: CI
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Test Action
      uses: ./
      with:
        TOKEN: ${{ secrets.GITHUB_TOKEN }}
